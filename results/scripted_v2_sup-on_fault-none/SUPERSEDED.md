# Superseded

Historical run at code revision `484d2bd` (2026-09-15). The teacher and the success
checks changed afterwards; notably the no-supervisor glitch row moved from 0/10 to
2/10 at commit `7767ee5`, when the teacher learned to re-grasp a dropped utensil.
Current numbers: `results/audit_final_*` (each has a manifest.json).
